apiVersion: v1
kind: Pod
metadata:
  name: pending-demo
  labels:
    app: pending-demo
spec:
  containers:
    - name: pending-demo-ctr
      image: nginx
      resources:
        requests:
          cpu: "100"
          memory: "128Gi"

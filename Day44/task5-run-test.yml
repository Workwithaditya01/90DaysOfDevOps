# Goal: Take any script from your earlier days (Python or Shell) and run it in CI

name: Run Test

on:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository code
      - name: Checkout
        uses: actions/checkout@v4

      # Install Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      # Install dependencies
      - name: Install Dependencies
        run: |
          pip install -r requirement.txt

      # Run Shell Script
      - name: Run Shell Script

